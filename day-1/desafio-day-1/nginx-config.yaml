apiVersion: v1
kind: Pod
metadata:
  labels:
    run: first-pod
  name: first-pod
spec:
  containers:
  - image: nginx
    name: first-pod
    ports:
    - containerPort: 80
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
